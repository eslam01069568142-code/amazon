# المرحلة الرابعة — نظام الاستيراد بالروابط المختصرة والنشر (Phase 4)

بعد الفحص الدقيق والـ (Read-Only Audit) لتدفق البيانات الحالي، تبين أن النظام **مصمم بالفعل بطريقة تطابق متطلباتك تماماً** بدون الحاجة لإضافة أي حقول أو تعديل جذري.

## تفاصيل الفحص (Audit Results)

1. **حقل التخزين (Affiliate Source URL):**
   * يوجد بالفعل حقل باسم `original_url` في جدول `products` (وفي النوع `Product` في `db.ts`).
   * هذا الحقل ليس بحاجة إلى أي تغيير، ولا نحتاج لإنشاء Migration جديد أو حقل جديد (`affiliate_link`)، لأن `original_url` يقوم بالضبط بهذه الوظيفة؛ فهو يخزن **الرابط الحرفي** الذي تم إدخاله في الـ Scraper.
2. **عملية الاستيراد (Scraper):**
   * عند إدخال رابط مثل `https://amzn.to/XXXXXXXX`، يقوم `route.ts` بالتقاط الجزء `XXXXXXXX` كمعرف فريد (ASIN).
   * يقوم بعمل `fetch` للرابط، والذي يتتبع تلقائياً الـ Redirect للوصول لصفحة أمازون الفعلية، ثم يسحب الصور والأسعار والتفاصيل بأعلى جودة.
   * ثم يحفظ المنتج في قاعدة البيانات محتفظاً بالرابط المختصر `https://amzn.to/XXXXXXXX` حرفياً داخل الحقل `original_url`.
3. **زر الشراء (Buy Button):**
   * صفحة `product/[id]/page.tsx` ومكون البطاقات تستخدم الدالة `getAmazonProductUrl`.
   * ملف `src/utils/amazon.ts` يحتوي حالياً على كود يقوم صراحة بإرجاع `originalUrl` **بدون أي تعديل وبدون استخدام `AMAZON_ASSOCIATE_ID` وبدون إضافة `?tag=`**.
4. **توجيه الزائر:**
   * عند الضغط على "شراء من أمازون"، يتوجه الزائر مباشرة إلى الرابط المخزن في `original_url` والذي يحمل الـ Affiliate ID الخاص بك بالفعل، تماماً كما أدخلته.

## User Review Required
> [!IMPORTANT]
> بما أن النظام يعمل حالياً بالشكل المثالي الذي طلبته، **لن يتم إجراء أي تعديل برمجي على مسار Affiliate لأنه سليم 100% ويطابق القواعد التي اشترطتها.** هل توافق على الاكتفاء بحذف المنتجات التجريبية وبدء النشر؟

## Proposed Changes

### 1. تنظيف قاعدة البيانات (Data Cleanup)
سيتم حذف المنتجات التجريبية الأربعة التي تم إدخالها في المرحلة الثالثة فقط، دون المساس بالتصنيفات أو الإعدادات:
* `prod_B0CHX2F5QT` (iPhone)
* `prod_B0BDHWDR12` (AirPods)
* `prod_B0863TXGM3` (Sony)
* `prod_B00Z9QVE4Q` (Power Bank)

### 2. اختبار الاستيراد (Import Test)
بانتظار أن تعطيني رابط Affiliate مختصر حقيقي خاص بك لاختباره للتأكد من أن الاستيراد يعمل ويتم تخزين الرابط المختصر وعرضه في زر الشراء.

### 3. النشر (Production Deployment)
بمجرد نجاح الاختبار:
* تشغيل `npm run build` للتأكد من استقرار النسخة.
* الرفع على خوادم Vercel المعتمدة باستخدام أوامر Vercel CLI المتوفرة في المشروع (`vercel --prod`).

## Verification Plan
* التأكد من أن عدد المنتجات أصبح `0` بعد الحذف.
* التأكد من نجاح اختبار الاستيراد برابط حقيقي تقدمه لي، وظهور الرابط بحالته الأصلية في زر الشراء.
* رفع المشروع وتأكيد النشر عبر رابط الـ Production.
